The ultimate website for collaboration between teachers, students and administrators.

Uses the API ClassroomSailorApi to get data.

In order to build
1. run the command 'npm install'
2. run 'webpack' 